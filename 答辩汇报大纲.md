# 答辩汇报大纲

> 总时长约20分钟。第1部分（背景）约3分钟，第2部分（主体）约12分钟，第3部分（专家意见）约2分钟，第4部分（总结）约1分钟，第5~7部分（个人情况）约2分钟。

---

## 第1部分：研究背景与意义（约3分钟）

### 页3｜分布式集群异常检测面临负载波动与阈值依赖双重挑战

> 各位老师好，下面我汇报我的硕士学位论文，题目是《基于多元时序的分布式集群异常检测研究》。

- 分布式集群是云计算和大数据处理的核心基础设施，节点故障会直接影响上层业务的连续性。集群运行中会产生海量的多维度监控时序数据，是感知系统状态、诊断异常的关键。
- 然而，异常检测面临两方面困难：一是**负载相关指标**（如RPC连接数）的正常范围随全局负载无规律波动，难以统一定义"正常"；二是传统**固定阈值**方法适应性差，基于深度学习的方法在负载变化时容易产生大量误报。
- 但我们在运维实践中观察到一个关键规律：在负载均衡机制下，正常运行时各节点的同一指标曲线高度相似。运维工程师日常排障也是采用类似的策略——对比多条节点曲线，重点排查偏离群体模式的"离群曲线"。

### 页4｜本文引入节点维度，构建三维分析范式

- 现有的时序异常检测研究主要围绕**时间和指标**两个维度展开，学习单节点的历史正常模式，在负载波动频繁时误报率高，且依赖大量标注数据来调优阈值。
- 本文的核心思路是在此基础上**引入集群节点作为第三个分析维度**，形成"时间×指标×节点"的三维分析范式。不再定义绝对的"正常范围"，而是通过对比节点间的行为差异来判断异常——偏离群体模式的节点即为异常候选。同时采用基于极值理论的方法自适应确定检测阈值，降低对标注数据的依赖。

### 页5｜本文三项主要贡献

- 围绕这一思路，本文做了三方面工作：
  - **第一**，针对单指标多节点场景，提出了CSAD-AT算法，融合多视角曲线相似性评分与I-SPOT自适应阈值，F1达到0.9864；
  - **第二**，针对多指标多节点场景，提出了NSFusion框架，通过数值和形状双视角融合进行检测，Acc@5达到83.81%；
  - **第三**，设计并实现了完整的集群节点异常检测与监控系统，支持常态化自动巡检和多级告警。

---

## 第2部分：论文主体（约12分钟）

### 页7｜论文研究内容框架图（约30秒）

- 这是论文的整体框架。全文共六章，核心研究内容分为三个模块：
  - 第三章解决**单指标多节点**场景，提出CSAD-AT算法；
  - 第四章扩展到**多指标多节点**场景，提出NSFusion框架；
  - 第五章将算法封装为**完整的检测系统**。
- 下面按这三个模块依次介绍。

---

### 第三章：CSAD-AT算法（约6分钟）

#### 页8｜联通YARN集群数据集（约1分钟）

- 首先介绍数据基础。数据来源于**中国联通大数据生产底座平台的YARN集群**，选取了RPC Router连接数作为核心指标。该指标直接反映各节点与ResourceManager之间的通信负载。
- 构建了训练集（30天正常数据）和测试集（8天，30个节点，含105个异常片段，异常比例约7.85%）。标注采用**多算法初筛加运维专家判读**的半人工方式。
- 如图所示，正常时各节点曲线高度同步、紧密缠绕，形成"正常行为簇"；异常节点（红色曲线）则显著偏离群体，呈现"**正常聚集、异常离群**"的鲜明特征，这正是我们方法的数据基础。

#### 页9｜基线算法评估（约45秒）

- 我们首先评估了六种主流的多元时序异常检测算法在本数据集上的表现。
- 结果显示，**所有基线的F1均低于0.70**。其中表现最好的MAD-GAN也只有0.6891。TranAD虽然精确率达到0.89，但召回率仅0.39，遗漏了大量真实异常。
- 根本原因在于：这些方法的设计思路是学习单节点的历史正常模式，而负载均衡类指标的"正常"随全局负载变化，历史模式在这里是失效的。这一结果验证了**需要一种不同的检测思路**。

#### 页10｜三种曲线相似性方法（约45秒）

- 基于"正常聚集、异常离群"的数据特征，我们提出了基于节点间曲线相似性的检测方法，从三个互补视角度量偏离程度：
  - **欧氏距离**：直接在数值空间度量节点间距离，F1 = 0.9741；
  - **自编码器嵌入**：在深度特征空间度量曲线形态差异，F1 = 0.9691；
  - **DBSCAN密度偏离**：从局部密度视角识别孤立节点，F1 = 0.9632。
- 三种方法在**最优参数**下F1均超过0.96，较基线提升超过30个百分点，充分验证了基于曲线相似性进行异常检测的有效性。
- 但关键问题是——这些优异性能建立在**精确的参数调优基础上**。

#### 页11｜阈值"悬崖效应"（约45秒）

- 参数敏感性实验揭示了一个严峻问题。如图所示，三种方法的F1随阈值变化都呈现"**先升后降**"的趋势，最优阈值处于极窄的区间内。
- 以欧氏距离方法为例，阈值从3.6调整到3.8，仅变化约2.8%，F1就从0.97**骤降至零**，呈现典型的"悬崖效应"。
- 更关键的是，不同集群的最优阈值不同，在一个集群上调优的参数无法直接迁移到另一个集群。而确定最优阈值需要大量标注数据，在生产环境中获取成本极高。
- 因此，**如何实现阈值的自适应调整**，成为将方法推向实际应用的关键。

#### 页12｜CSAD-AT算法框架（约30秒）

- 针对上述问题，我们提出了CSAD-AT算法，包含三个核心模块：
  - **多视角相似性评分**：融合欧氏距离、自编码器嵌入和密度偏离三种互补评分；
  - **分数归一化与聚合**：将不同量纲的评分统一归一化后加权融合；
  - **I-SPOT自适应阈值**：基于极值理论动态确定异常判定边界，无需人工设定阈值。

#### 页13｜I-SPOT vs 经典SPOT（约1分钟）

- I-SPOT是本文的关键改进。经典SPOT算法在更新模型时**排除被判为异常的数据点**，在概念漂移场景下会导致数据池不断收缩、阈值持续下降，形成"阈值衰减"的恶性循环。
- 如上图所示，经典SPOT的阈值（橙色线）在初期尚可，但随着处理推进单调下降，后半段几乎所有数据点都被标记为异常，精确率仅0.32。
- I-SPOT的核心改进是**包容性更新策略**：所有数据点无论是否超过阈值，均纳入FIFO数据池，并周期性重新拟合GPD分布、更新阈值。
- 如下图所示，I-SPOT的阈值始终保持平稳，精确率达到1.00，F1从0.4870提升到0.9864，提升约**50个百分点**。

#### 页14｜CSAD-AT实验结果（约30秒）

- 汇总来看，CSAD-AT在**无需人工阈值调整**的条件下，精确率1.0、召回率0.9732、F1达到0.9864。
- 这个成绩不仅优于所有六种基线算法，也优于各单方法在最优人工阈值下的表现（如欧氏距离的0.9741），还远超经典SPOT组合的0.4870。
- 消融实验也证实了三种视角的互补性：融合后召回率从单方法最优的0.92提升到了0.97。

---

### 第四章：NSFusion框架（约4分钟）

#### 页15｜多指标场景新挑战（约45秒）

- 第三章验证了单指标场景的有效性，但真实集群需要同时监控多种指标。从单指标扩展到多指标面临两方面新挑战：
  - **效率约束**：GPU集群有10多个指标、500到700个节点，自编码器需要逐指标训练，计算开销大幅增长；
  - **异常异质性**：如图所示，同一次故障中不同指标的表现差异很大——左图是核心异常指标，偏离明显；右图是弱相关指标，异常节点几乎与正常节点重合。仅靠数值距离的单一视角存在检测盲区。

#### 页16｜NSFusion框架（约30秒）

- 针对这两方面挑战，我们提出了NSFusion框架，即**数值与形状双视角融合**。
- 框架分为三个阶段：第一阶段从**数值特征**角度，基于欧氏距离计算各指标的异常分数并加权融合；第二阶段从**形状特征**角度，采用改进的FR-SAX符号化算法捕捉曲线形态差异；第三阶段将两个视角的结果**加权融合**，输出各节点的最终异常评分。
- 整个框架放弃了自编码器和密度偏离等计算密集的方法，采用轻量化方案，无需模型训练。

#### 页17｜FR-SAX改进（约45秒）

- FR-SAX是本文提出的形状特征提取方法，对传统SAX做了两项针对性改进：
  - **第一**，取消分段聚合，保留原始时间分辨率。传统SAX通过分段均值降维，但集群故障的早期征兆往往是短暂的瞬时尖峰或抖动，均值化会将这些信号淹没。FR-SAX直接对每个数据点逐一符号化，完整保留细粒度信息。
  - **第二**，采用严格的符号距离计算规则。传统SAX将相邻符号距离设为0，而故障信号可能恰好表现为系统性的单级偏差。FR-SAX中只要符号不同就计算实际距离，提升对细微形态差异的敏感性。
- FR-SAX无需训练、计算高效，是自编码器在多指标场景下的轻量化替代方案。

#### 页18｜NSFusion实验结果（约45秒）

- 在包含**105个真实GPU集群故障案例**的数据集上进行验证，故障类型涵盖GPU故障、网络故障、PCIe故障等。
- NSFusion融合方法的Acc@5达到**0.8381**、Avg Rank为**1.18**，均为最优。
- 与马氏距离基线（Acc@5 = 0.4532）相比提升巨大；与CVAE深度生成模型（Acc@5 = 0.8376）相比，检测率持平而定位精度更优（Avg Rank 1.18 vs 1.46）。
- 关键优势在于：NSFusion**无需训练、仅需CPU、可即插即用**，在部署灵活性和可解释性方面显著优于CVAE。

---

### 第五章：检测系统（约1分钟）

#### 页19｜系统架构与功能

- 在算法研究基础上，我们设计了集群节点异常检测与监控系统。
- 系统采用**前后端分离的四层架构**：用户层提供Web可视化界面，应用层基于Flask构建API服务，算法层封装CSAD-AT和NSFusion两套检测引擎，数据层使用SQLite存储。
- 五个核心功能模块：数据接入（支持文件上传和Prometheus API）、检测引擎、常态化监控（APScheduler定时巡检）、告警管理（三级告警 + 邮件/企业微信通知）、可视化看板。
- 系统已具备对接联通生产环境Prometheus监控体系的接口能力，**单次巡检端到端耗时约20秒**，满足分钟级巡检间隔的要求。

---

## 第3部分：评阅专家意见应答（约2分钟）

### 页21｜主要学术意见及修改

> 下面汇报三位评阅专家的意见及修改情况。

- **专家一**指出第五章缺少系统整体框架图，各模块关系展现不够直观。我们新增了系统总述段落和图5-1系统架构图，明确了CSAD-AT和NSFusion在系统中的集成方式，并强化了第三、四章与第五章之间的衔接。

- **专家二**提出两个学术问题：
  - 第一个是关于第3.3.3节DBSCAN方法与第3.4.2节LOF方法的关系。这两节实际上是**从基础方法到算法设计的递进关系**——DBSCAN验证了密度分析视角的有效性，但其离散二值输出无法满足多视角融合框架对连续评分的要求，因此CSAD-AT改用了基于k近邻可达密度的连续评分方法。已在3.4.2节补充了设计动机说明。
  - 第二个是第四章缺少同类型基线对比。我们补充了**CVAE条件变分自编码器**作为深度生成模型类基线，修改后的实验涵盖了经典统计方法、深度学习方法和本文方法的消融实验。

- **专家三**指出章标题中未体现"时序"概念。已将第三章和第四章标题分别修改为含"时序"的新标题，与论文大题目形成呼应。

### 页22｜写作规范意见及修改

- 三位专家均指出了写作规范方面的问题，已全文排查并系统性修正：
  - **AI写作痕迹**：清理了8处"具体而言""值得注意的是"等程式化过渡词，重写了对仗式评述和过度拔高的用语，涉及第一、三、四章。
  - **图片规范**：统一了全文图片的字体大小和加粗样式。
  - **过短段落**：合并了第三、四、五章中多处单句独立成段的情况。
  - **公式标点**：规范了第三章12处公式的标点与后续文本缩进关系。
  - **摘要用词**：将"基于多元时序曲线相似性"修正为与论文标题一致的表述。

---

## 第4部分：总结与展望（约1分钟）

### 页24｜三项贡献与未来方向

- 总结来说，本文围绕分布式集群多节点时序异常检测问题，做了三方面贡献：
  - 提出了CSAD-AT算法，在单指标场景下实现了F1 = 0.9864的自适应检测；
  - 提出了NSFusion框架，在多指标场景下实现了Acc@5 = 83.81%的轻量化检测；
  - 设计了完整的检测系统，支持常态化巡检和多级告警。
- 未来有三个值得深入的方向：一是引入图神经网络建模节点拓扑关系，适配异构集群；二是研究在线增量学习机制，应对概念漂移；三是结合多源数据进行异常根因分析，构建智能运维闭环。

> 以上就是我的汇报，请各位老师批评指正。

---

## 第5~7部分：个人情况（约2分钟）

*（课程学习、参与项目及发表论文、致谢——待补充具体内容。）*
